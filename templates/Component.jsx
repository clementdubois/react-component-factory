"use strict";
const React = require("react");

module.exports = React.createClass({
 displayName : "{{name}}",

 propTypes : {},

 render : function render() {
   return (
     <div className="{{name}}">
     </div>
   );
 },

});
